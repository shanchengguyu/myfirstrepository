server:
  port: 12343

jwt:
  config:
    secret: abcdefghijklmnopqrstuvwxyz  # 加密的secret
    ttl: 3000  # token超时时间 秒

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/user?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf8&autoReconnect=true&allowMultiQueries=true
    username: root
    password: root